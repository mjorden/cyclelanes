% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{cl_components}
\alias{cl_components}
\title{Connected components of the low-stress network}
\usage{
cl_components(x, max_lts = NULL, tolerance = 5)
}
\arguments{
\item{x}{An \code{sf} from \code{\link[=cl_classify]{cl_classify()}}, \code{\link[=cl_bike_lanes]{cl_bike_lanes()}},
\code{\link[=cl_fetch_official]{cl_fetch_official()}} or \code{\link[=cl_lts]{cl_lts()}}.}

\item{max_lts}{Keep segments with \code{lts} at or below this (1-4), running
\code{\link[=cl_lts]{cl_lts()}} first if needed. \code{NULL} keeps everything.}

\item{tolerance}{Snap distance in metres.}
}
\value{
The kept segments as an \code{sf} (WGS84) with an integer \code{component}
column, plus \code{attr(x, "components")}: a data frame of \code{component},
\code{n_segments}, \code{length_km}, \code{share}.
}
\description{
How fragmented is the network a given kind of rider can use? Keeps the
facilities at or below \code{max_lts} (all of them when \code{NULL}), builds the
network with \code{\link[=cl_as_sfnetwork]{cl_as_sfnetwork()}}, and labels each segment with the
connected component it belongs to, numbered from the longest. The
component table in \code{attr(x, "components")} gives the number of segments
and kilometres in each, so "the largest low-stress island is 12 km out
of 260" is one call.
}
\examples{
if (requireNamespace("sfnetworks", quietly = TRUE)) {
  lanes <- cl_classify(denver_lodo$osm, drop_none = TRUE)
  low <- cl_components(lanes, max_lts = 2)
  head(attr(low, "components"))
}
}
