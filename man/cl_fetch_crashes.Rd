% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crashes.R
\name{cl_fetch_crashes}
\alias{cl_fetch_crashes}
\title{Fetch reported crashes for a city}
\usage{
cl_fetch_crashes(
  city = "denver",
  years = NULL,
  bike_only = TRUE,
  bbox = NULL,
  keep_raw = FALSE
)
}
\arguments{
\item{city}{A key from \code{\link[=cl_crash_sources]{cl_crash_sources()}}. Default \code{"denver"}.}

\item{years}{Optional integer vector of years to keep, e.g. \code{2019:2025}.}

\item{bike_only}{Keep bicycle-involved crashes only (default). \code{FALSE}
returns every crash, which for a city is hundreds of thousands.}

\item{bbox}{Optional area (anything \code{\link[=cl_bbox]{cl_bbox()}} accepts, or an \code{sf}
polygon, which also clips).}

\item{keep_raw}{Keep the source's own columns alongside the standard ones.}
}
\value{
An \code{sf} of \code{POINT}s in WGS84 with columns \code{source_id}, \code{date},
\code{year}, \code{severity} (factor \code{fatal} > \code{serious} > \code{minor} > \code{none} >
\code{unknown}), \code{bicycle}, \code{pedestrian}, \code{n_fatal}, \code{n_serious},
\code{injured_mode} (\code{bicycle}, \code{pedestrian}, \code{motor_vehicle}, \code{unknown}:
who was hurt, as distinct from who was involved), \code{location_type}
(\code{intersection}, \code{mid_block}, \code{unknown}), \code{light}, \code{road_condition},
\code{address}. Attributes \code{cl_source}, \code{cl_attribution}, \code{cl_fetched}.
}
\description{
Reads a registered crash source (see \code{\link[=cl_crash_sources]{cl_crash_sources()}}) and returns
the crashes as points with a standard set of columns, so
\code{\link[=cl_crash_join]{cl_crash_join()}} and \code{\link[=cl_crash_rates]{cl_crash_rates()}} work on any city.
}
\details{
Police crash data miss roughly half of bicycle injury crashes and
almost all crashes with no motor vehicle involved; what comes back is
\emph{reported} crashes. Sources are revised continuously, so the fetch time
in \code{attr(x, "cl_fetched")} is part of the result.
}
\examples{
\dontrun{
crashes <- cl_fetch_crashes("denver", years = 2019:2025)
table(crashes$severity, crashes$location_type)
}
}
