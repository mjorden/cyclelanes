% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{cl_map}
\alias{cl_map}
\title{Interactive map of classified bike lanes}
\usage{
cl_map(
  x,
  colour = c("facility_type", "lts"),
  unmatched_below = 0.5,
  weight = 3
)
}
\arguments{
\item{x}{An \code{sf} from \code{\link[=cl_classify]{cl_classify()}}, \code{\link[=cl_bike_lanes]{cl_bike_lanes()}},
\code{\link[=cl_fetch_official]{cl_fetch_official()}} or \code{\link[=cl_lts]{cl_lts()}}, or a \code{cl_comparison}.}

\item{colour}{\code{"facility_type"} (default) or \code{"lts"}.}

\item{unmatched_below}{For a comparison, segments with \code{matched_frac}
below this are drawn in a separate "not in the other layer" group.}

\item{weight}{Line weight in pixels.}
}
\value{
A \code{leaflet} htmlwidget.
}
\description{
A \code{leaflet} map coloured by \code{facility_type} (or by \code{lts}), with a popup
per segment that links to the way on openstreetmap.org so a mapper can
fix a tag straight from the map. Given a \code{\link[=cl_compare]{cl_compare()}} result, draws
the OSM and official layers as toggleable groups and highlights the
segments the other layer lacks. Requires \code{leaflet}.
}
\examples{
if (requireNamespace("leaflet", quietly = TRUE)) {
  cl_map(cl_classify(denver_lodo$osm, drop_none = TRUE))
}
}
