% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crash_rates.R
\name{cl_crash_rates}
\alias{cl_crash_rates}
\title{Reported crashes per kilometre-year by facility type}
\usage{
cl_crash_rates(joined, years = NULL, by = "facility_type")
}
\arguments{
\item{joined}{A \code{cl_crash_join}.}

\item{years}{The calendar years the crash data cover, e.g. \code{2019:2025}.
Taken from the crash dates when \code{NULL}.}

\item{by}{Facility column(s) to group by. Default \code{"facility_type"}.}
}
\value{
A data frame with one row per group and \code{location_type}:
\code{length_km}, \code{km_years}, \code{n_crashes}, \code{n_serious_fatal},
\code{crashes_per_km_year}, \code{serious_fatal_per_km_year}, and, when
\code{exposure} is present, \code{crashes_per_million_bike_km}. Crashes not
within tolerance of any facility appear under \code{facility_type = "none"}
with no denominator.
}
\description{
Turns a \code{\link[=cl_crash_join]{cl_crash_join()}} into rates that can be compared across
facility types. The denominator is \strong{kilometre-years}: facility
kilometres times the years the crash data span (or, when the facilities
carry an \code{install_year}, only the years after each was installed).
Intersection and mid-block crashes are reported on separate rows and
never merged: intersection crashes attach to whichever facility passes
through the intersection.
}
\details{
Crashes per kilometre-year is not crashes per rider. A busy protected
lane carries many times the riders of a quiet sharrow, so a raw
per-kilometre rate flatters low-use facilities. When the facilities
carry an \code{exposure} column (average daily bicycle volume) the output
also gives crashes per million bicycle-kilometres. Police data miss
roughly half of bicycle injury crashes; these are reported crashes.
}
