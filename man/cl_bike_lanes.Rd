% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bike_lanes.R
\name{cl_bike_lanes}
\alias{cl_bike_lanes}
\title{Bike lanes for a place, fetched from OpenStreetMap and classified}
\usage{
cl_bike_lanes(
  place,
  drop_none = TRUE,
  keep_tags = FALSE,
  strict = FALSE,
  timeout = 180,
  clip = TRUE,
  ...
)
}
\arguments{
\item{place}{One of:
\itemize{
\item a place name geocoded through Nominatim, e.g. \code{"Denver, Colorado"}
(respect the Nominatim usage policy: at most one request per second,
no bulk geocoding);
\item a numeric vector \code{c(xmin, ymin, xmax, ymax)} in WGS84 degrees;
\item an \code{sf}, \code{sfc}, or \code{bbox} object in any CRS;
\item the 2 x 2 matrix returned by \code{\link[osmdata:getbb]{osmdata::getbb()}}.
}}

\item{drop_none}{Drop ways whose overall \code{facility_type} is \code{none}.}

\item{keep_tags}{Keep every input column in addition to the standard
output columns. By default only \code{osm_id}, \code{name}, \code{highway}, \code{oneway}
and \code{surface} survive from the input.}

\item{strict}{Also drop \code{shared_use_path} ways that are not
\code{highway=cycleway} and lack a paved \code{surface} tag (\code{paved}, \code{asphalt},
\code{concrete}, \code{paving_stones}, ...). Park footways that a mapper tagged
bike-friendly are the bulk of what this removes; use it when you want
on-road-quality kilometres only.}

\item{timeout}{Overpass server timeout in seconds. City-scale queries can
take a minute or more; the public Overpass instance rejects long-running
queries, so keep \code{place} to a metro area or smaller.}

\item{clip}{Overpass only takes a bounding box, and the box around a city
usually spills into its neighbours. \code{TRUE} (the default) clips the result
to the place's administrative boundary from \code{\link[=cl_boundary]{cl_boundary()}} when \code{place}
is a name; ways crossing the boundary are cut at it. Pass an \code{sf}/\code{sfc}
polygon to clip to your own boundary with any kind of \code{place}, or
\code{FALSE} to keep the whole box. Ignored for bbox and \code{sf} inputs unless a
polygon is supplied.}

\item{...}{Passed to \code{\link[=cl_fetch_osm]{cl_fetch_osm()}}: \code{overpass_url}, \code{retries}, \code{tile},
\code{cache}, \code{cache_dir}, \code{cache_max_age}.}
}
\value{
The classified \code{sf} described in \code{\link[=cl_classify]{cl_classify()}}, with the queried
bounding box in \code{attr(x, "cl_bbox")}, the fetch time in
\code{attr(x, "cl_fetched")}, and \code{attr(x, "cl_source") == "openstreetmap"}.
}
\description{
Convenience wrapper: \code{\link[=cl_fetch_osm]{cl_fetch_osm()}} followed by \code{\link[=cl_classify]{cl_classify()}}, with
non-facility ways dropped by default.
}
\examples{
\dontrun{
denver <- cl_bike_lanes("Denver, Colorado")
cl_summary(denver)
cl_plot(denver)
}
}
