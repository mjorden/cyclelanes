% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bike_lanes.R
\name{cl_timeline}
\alias{cl_timeline}
\title{How a place's bike network has grown}
\usage{
cl_timeline(place, dates, by = "facility_type", clip = TRUE, cache = TRUE, ...)
}
\arguments{
\item{place}{One of:
\itemize{
\item a place name geocoded through Nominatim, e.g. \code{"Denver, Colorado"}
(respect the Nominatim usage policy: at most one request per second,
no bulk geocoding);
\item a numeric vector \code{c(xmin, ymin, xmax, ymax)} in WGS84 degrees;
\item an \code{sf}, \code{sfc}, or \code{bbox} object in any CRS;
\item the 2 x 2 matrix returned by \code{\link[osmdata:getbb]{osmdata::getbb()}}.
}}

\item{dates}{A vector of \code{Date}s or \code{"YYYY-MM-DD"} strings.}

\item{by}{Passed to \code{\link[=cl_summary]{cl_summary()}}.}

\item{clip}{Overpass only takes a bounding box, and the box around a city
usually spills into its neighbours. \code{TRUE} (the default) clips the result
to the place's administrative boundary from \code{\link[=cl_boundary]{cl_boundary()}} when \code{place}
is a name; ways crossing the boundary are cut at it. Pass an \code{sf}/\code{sfc}
polygon to clip to your own boundary with any kind of \code{place}, or
\code{FALSE} to keep the whole box. Ignored for bbox and \code{sf} inputs unless a
polygon is supplied.}

\item{cache}{Store the raw Overpass result under \code{cache_dir} and reuse it
for the same bounding box within \code{cache_max_age} days.}

\item{...}{Passed to \code{\link[=cl_bike_lanes]{cl_bike_lanes()}}: \code{drop_none}, \code{strict}, \code{timeout},
\code{retries}, \code{overpass_url}, \code{tile}, \code{cache_dir}, \code{cache_max_age}.}
}
\value{
A data frame: \code{date}, then the columns of \code{\link[=cl_summary]{cl_summary()}}. A date
whose fetch fails is skipped with a warning.
}
\description{
Fetches the network as it was on each of \code{dates} through Overpass's
attic data, classifies it, and stacks the \code{\link[=cl_summary]{cl_summary()}} of each
snapshot with a \code{date} column: kilometres by facility type over time.
}
\details{
Attic queries are slow and the public server rate-limits them, so use
yearly steps, keep \code{cache = TRUE} (the default here), and expect a
whole city to take a few minutes per date. Snapshots before roughly
2012 are thin because attic data starts with the ODbL licence change.
}
\examples{
\dontrun{
growth <- cl_timeline("Boulder, Colorado", dates = paste0(2014:2026, "-01-01"))
subset(growth, facility_type == "protected_lane")
}
}
