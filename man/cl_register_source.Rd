% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/official.R
\name{cl_register_source}
\alias{cl_register_source}
\title{Register an official bike-facility source for a city}
\usage{
cl_register_source(
  city,
  url,
  class_field,
  crosswalk,
  type = c("arcgis", "file"),
  label = city,
  id_field = NULL,
  name_field = NULL,
  extra_fields = NULL,
  existing_where = NULL,
  existing_filter = NULL,
  attribution = NULL,
  homepage = NULL
)
}
\arguments{
\item{city}{Short lower-case key, e.g. \code{"portland"}.}

\item{url}{Layer endpoint or file location (see Details).}

\item{class_field}{Name of the attribute holding the facility class.}

\item{crosswalk}{Named character vector mapping each value of \code{class_field}
to a level of \code{\link[=cl_facility_levels]{cl_facility_levels()}}. Unmapped values become \code{none}
with a warning at fetch time.}

\item{type}{\code{"arcgis"} or \code{"file"}.}

\item{label}{Human-readable name.}

\item{id_field, name_field}{Optional attribute names for a stable feature
id and a street/route name.}

\item{extra_fields}{Optional named character vector: output column name
= source attribute name, copied through verbatim.}

\item{existing_where}{For \code{"arcgis"} sources, an SQL where clause that
restricts to built (not planned) facilities.}

\item{existing_filter}{A function taking the raw \code{sf} and returning a
logical vector; applied after reading for either type.}

\item{attribution, homepage}{Provenance strings reported by \code{\link[=cl_sources]{cl_sources()}}.}
}
\value{
The registered source definition, invisibly.
}
\description{
Adds (or replaces) a source in the session registry so that
\link[=cl_fetch_official]{cl_fetch_official(city)} works for it. Two source
types are supported:
}
\details{
\itemize{
\item \code{"arcgis"}: \code{url} is an ArcGIS REST \strong{layer} endpoint
(\verb{.../FeatureServer/<n>} or \verb{.../MapServer/<n>}). Features are paged
through the \code{query} operation as GeoJSON. \code{existing_where} is an SQL
\code{where} clause applied server-side.
\item \code{"file"}: \code{url} is anything \code{\link[sf:st_read]{sf::st_read()}} can open -- a local path or a
direct URL to GeoJSON, a zipped shapefile, a GeoPackage, and so on.
}
}
\examples{
cl_register_source(
  "exampleville",
  url = "https://example.org/bikeways.geojson",
  type = "file",
  class_field = "FACILITY",
  crosswalk = c("Protected Bike Lane" = "protected_lane",
                "Bike Lane" = "painted_lane",
                "Multi-Use Trail" = "separated_path")
)
cl_sources()
}
