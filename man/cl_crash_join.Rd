% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crash_rates.R
\name{cl_crash_join}
\alias{cl_crash_join}
\title{Snap crashes to the nearest facility}
\usage{
cl_crash_join(lanes, crashes, tolerance = 25)
}
\arguments{
\item{lanes}{An \code{sf} of facilities from \code{\link[=cl_classify]{cl_classify()}},
\code{\link[=cl_bike_lanes]{cl_bike_lanes()}} or \code{\link[=cl_fetch_official]{cl_fetch_official()}}.}

\item{crashes}{An \code{sf} of points from \code{\link[=cl_fetch_crashes]{cl_fetch_crashes()}}.}

\item{tolerance}{Snap distance in metres.}
}
\value{
A list of class \code{cl_crash_join}: \code{crashes} (WGS84, with
\code{facility_row}, \code{facility_type}, \code{snap_dist_m} added), \code{facilities}
(WGS84, with \code{n_crashes}, \code{n_serious}, \code{n_fatal} added), and
\code{tolerance}.
}
\description{
Attaches each crash to the nearest facility within \code{tolerance} metres,
or to \code{facility_type = "none"} when nothing is that close, and counts
crashes per facility. Intersection crashes are coded to the
intersection point and will attach to whichever facility passes through
it; keep \code{location_type} in every summary rather than merging it away.
}
\examples{
\dontrun{
lanes <- cl_fetch_official("denver")
crashes <- cl_fetch_crashes("denver", years = 2019:2025)
j <- cl_crash_join(lanes, crashes)
cl_crash_rates(j)
}
}
