% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crashes.R
\name{cl_register_crash_source}
\alias{cl_register_crash_source}
\title{Register a crash data source for a city}
\usage{
cl_register_crash_source(
  city,
  url,
  fields,
  type = c("arcgis", "file"),
  bike_where = NULL,
  label = city,
  attribution = NULL,
  homepage = NULL
)
}
\arguments{
\item{city}{Short lower-case key.}

\item{url}{An ArcGIS REST layer endpoint, or anything \code{\link[sf:st_read]{sf::st_read()}}
can open.}

\item{fields}{A named list mapping standard names to source attribute
names: \code{id}, \code{date} (a date, epoch milliseconds, or ISO string),
\code{bicycle} and \code{pedestrian} (flags: \code{1}, \code{Y}, \code{yes}, \code{TRUE}),
\code{n_fatal}, \code{n_serious} (counts), \code{injured_mode} (one or more fields
naming the mode of the people hurt: values containing \code{BIC}, \code{PED}),
\code{location_type} (values containing \code{INTERSECTION} are intersections),
\code{light}, \code{road_condition}, \code{address}, \code{severity_text} (a free-text
offense or severity in which \code{INJUR} means at least a minor injury),
and \code{lon}/\code{lat} for sources whose geometry can be empty.}

\item{type}{\code{"arcgis"} or \code{"file"}.}

\item{bike_where}{For ArcGIS sources, an SQL where clause selecting
bicycle-involved crashes server-side; for file sources the \code{bicycle}
flag is used instead.}

\item{label, attribution, homepage}{Provenance strings.}
}
\value{
The source definition, invisibly.
}
\description{
Adds (or replaces) a source so that \link[=cl_fetch_crashes]{cl_fetch_crashes(city)}
works for it. \code{fields} names the source's attributes for each standard
column; every entry is optional except \code{date}, and missing ones come
through as \code{NA}.
}
