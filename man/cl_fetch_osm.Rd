% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_osm.R
\name{cl_fetch_osm}
\alias{cl_fetch_osm}
\title{Fetch raw bicycle-related ways from OpenStreetMap}
\usage{
cl_fetch_osm(
  place,
  timeout = 180,
  clip = TRUE,
  overpass_url = NULL,
  retries = 3,
  tile = NULL,
  cache = FALSE,
  cache_dir = cl_cache_dir(),
  cache_max_age = 30,
  backend = c("overpass", "extract"),
  extract_dir = NULL,
  max_extract_mb = 1000
)
}
\arguments{
\item{place}{One of:
\itemize{
\item a place name geocoded through Nominatim, e.g. \code{"Denver, Colorado"}
(respect the Nominatim usage policy: at most one request per second,
no bulk geocoding);
\item a numeric vector \code{c(xmin, ymin, xmax, ymax)} in WGS84 degrees;
\item an \code{sf}, \code{sfc}, or \code{bbox} object in any CRS;
\item the 2 x 2 matrix returned by \code{\link[osmdata:getbb]{osmdata::getbb()}}.
}}

\item{timeout}{Overpass server timeout in seconds. City-scale queries can
take a minute or more; the public Overpass instance rejects long-running
queries, so keep \code{place} to a metro area or smaller.}

\item{clip}{Overpass only takes a bounding box, and the box around a city
usually spills into its neighbours. \code{TRUE} (the default) clips the result
to the place's administrative boundary from \code{\link[=cl_boundary]{cl_boundary()}} when \code{place}
is a name; ways crossing the boundary are cut at it. Pass an \code{sf}/\code{sfc}
polygon to clip to your own boundary with any kind of \code{place}, or
\code{FALSE} to keep the whole box. Ignored for bbox and \code{sf} inputs unless a
polygon is supplied.}

\item{overpass_url}{Overpass endpoint for this call only, e.g.
\code{"https://overpass.private.coffee/api/interpreter"}. Must end in
\verb{/interpreter}. The session-wide default set with
\code{\link[osmdata:set_overpass_url]{osmdata::set_overpass_url()}} is restored afterwards.}

\item{retries}{How many times to retry a transient failure (429, 502,
503, 504, timeout) before giving up. Waits start at 30 s and double,
with jitter, capped at 5 minutes.}

\item{tile}{Split the bounding box into tiles of at most this many
degrees on a side and query each, de-duplicating ways by \code{osm_id}.
\code{NULL} (default) sends one query. Use around \code{0.25} for areas that
time out; note each tile is a separate request against the server's
per-address rate limit.}

\item{cache}{Store the raw Overpass result under \code{cache_dir} and reuse it
for the same bounding box within \code{cache_max_age} days.}

\item{cache_dir}{Directory for cached results; see \code{\link[=cl_cache_dir]{cl_cache_dir()}}.}

\item{cache_max_age}{Maximum age in days of a cached result to reuse.}

\item{backend}{\code{"overpass"} (default) queries the Overpass API for the
bounding box. \code{"extract"} downloads the smallest Geofabrik extract
covering the box through the \code{osmextract} package, converts it once,
and reads the ways from there: no Overpass request at all, so no rate
limit, and the second place in the same region is instant. The first
download of a US state is 50-300 MB and the conversion takes a few
minutes; extracts lag OSM by about a day. Right for a city or a batch
of cities; Overpass is right for a neighbourhood.}

\item{extract_dir}{Where downloaded extracts and their converted
GeoPackages are kept. Defaults to an \code{extracts} folder under
\code{\link[=cl_cache_dir]{cl_cache_dir()}}, so a region is downloaded once per machine, not once
per session.}

\item{max_extract_mb}{Refuse to download an extract larger than this many
megabytes. The extract is chosen as the smallest Geofabrik region that
fully contains the clip boundary (or the bounding box when there is
none); a box that straddles a state line can match a whole multi-state
region, and this stops that quietly turning into a multi-gigabyte
download.}
}
\value{
An \code{sf} object of \code{LINESTRING}s in WGS84 (EPSG:4326) with the raw
OSM tag columns present in the area. A zero-row \code{sf} if nothing matched.
The WGS84 bounding box queried is stored in \code{attr(x, "cl_bbox")}, the
clipping polygon (if any) in \code{attr(x, "cl_boundary")}, and the fetch
time in \code{attr(x, "cl_fetched")} (the original fetch time for a cache hit).
}
\description{
Queries the Overpass API for every way inside \code{place} that carries a tag
which can denote bicycle infrastructure: \code{highway=cycleway}, any
\code{cycleway}, \code{cycleway:left}, \code{cycleway:right} or \code{cycleway:both} tag,
\code{bicycle=designated}, \code{bicycle_road=yes}, or \code{cyclestreet=yes}. The result
is \emph{unclassified}: many of these ways (a footway with \code{bicycle=designated},
a road tagged \code{cycleway=no}) are not facilities. Pass it to
\code{\link[=cl_classify]{cl_classify()}} or use \code{\link[=cl_bike_lanes]{cl_bike_lanes()}} which does both.
}
\details{
Overpass is a shared public service that answers \verb{HTTP 429} when an
address queries too often and \code{504} when it is overloaded. Those are
retried with exponential backoff (\code{retries}), a mirror can be used for
one call (\code{overpass_url}), large areas can be split into tiles (\code{tile}),
and results can be cached on disk (\code{cache}) so a re-run of a script does
not hit the server at all.
}
\examples{
\dontrun{
raw <- cl_fetch_osm(c(-105.00, 39.74, -104.98, 39.75))
nrow(raw)

# a mirror, with caching so the next run is instant
raw <- cl_fetch_osm("Boulder, Colorado", cache = TRUE,
                    overpass_url = "https://overpass.private.coffee/api/interpreter")
}
}
