% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lts.R
\name{cl_lts}
\alias{cl_lts}
\title{Level of Traffic Stress}
\usage{
cl_lts(x)
}
\arguments{
\item{x}{An \code{sf} from \code{\link[=cl_classify]{cl_classify()}} or \code{\link[=cl_bike_lanes]{cl_bike_lanes()}}.}
}
\value{
\code{x} with integer \code{lts} (1-4) and character \code{lts_basis} columns
added.
}
\description{
Scores each facility 1 (comfortable for almost anyone) to 4 (strong and
fearless only) following the Level of Traffic Stress criteria of
Mekuria, Furth and Nixon (2012) as simplified in Furth's 2017 tables.
The score depends on the facility type and, for on-street facilities,
on the parent road's speed limit and number of lanes, which
\code{\link[=cl_classify]{cl_classify()}} carries as \code{road_maxspeed_kph} and \code{road_lanes}.
}
\details{
Where a tag is missing the road class stands in: a \code{residential} or
\code{living_street} road is assumed 40 km/h with 2 lanes, a \code{tertiary} road
48 km/h with 2 lanes, \code{secondary} 56 km/h with 4, \code{primary} or \code{trunk}
64 km/h with 4, anything else 48 km/h with 2. \code{lts_basis} records which
rule fired and whether an assumption was used, so scores built on
assumptions can be told from scores built on tags. Parking-lane
conflicts and intersection approaches, which the full method also
scores, are not modelled.

The rule table ships as \code{system.file("extdata", "lts_rules.csv", package = "cyclelanes")} and is documented there.
}
\examples{
lanes <- cl_lts(cl_classify(denver_lodo$osm, drop_none = TRUE))
table(lanes$lts)
cl_summary(lanes, by = "lts")
}
\references{
Mekuria, M. C., Furth, P. G., & Nixon, H. (2012). Low-stress
bicycling and network connectivity. Mineta Transportation Institute
Report 11-19. Furth, P. G. (2017). Level of Traffic Stress criteria for
road segments, version 2.0.
}
