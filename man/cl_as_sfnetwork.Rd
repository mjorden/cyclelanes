% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{cl_as_sfnetwork}
\alias{cl_as_sfnetwork}
\title{Build a routable network from classified facilities}
\usage{
cl_as_sfnetwork(x, tolerance = 5)
}
\arguments{
\item{x}{An \code{sf} from \code{\link[=cl_classify]{cl_classify()}}, \code{\link[=cl_bike_lanes]{cl_bike_lanes()}},
\code{\link[=cl_fetch_official]{cl_fetch_official()}} or \code{\link[=cl_lts]{cl_lts()}}.}

\item{tolerance}{Snap distance in metres.}
}
\value{
An \code{sfnetwork} in a projected (UTM) CRS.
}
\description{
Turns the line layer into an undirected \code{sfnetworks} graph whose edges
carry \code{facility_type}, \code{length_m} and, if present, \code{lts}. Endpoints
within \code{tolerance} metres of each other are snapped to one node so
facilities that meet at an intersection connect even when their ends
do not coincide exactly. Requires \code{sfnetworks}.
}
\examples{
if (requireNamespace("sfnetworks", quietly = TRUE)) {
  net <- cl_as_sfnetwork(cl_classify(denver_lodo$osm, drop_none = TRUE))
  net
}
}
