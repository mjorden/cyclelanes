% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbox.R
\name{cl_boundary}
\alias{cl_boundary}
\title{Administrative boundary polygon for a place}
\usage{
cl_boundary(place)
}
\arguments{
\item{place}{A place name, or an \code{sf}/\code{sfc} polygon which is returned
as-is (unioned, in WGS84) so callers can supply their own boundary.}
}
\value{
An \code{sf} with one row: \code{name} (Nominatim's display name), \code{osm_type},
\code{osm_id}, and a \code{POLYGON}/\code{MULTIPOLYGON} geometry in EPSG:4326.
}
\description{
Geocodes \code{place} through Nominatim with \code{polygon_geojson=1} and returns
the matching boundary as an \code{sf} polygon. Used by \code{\link[=cl_fetch_osm]{cl_fetch_osm()}} and
\code{\link[=cl_bike_lanes]{cl_bike_lanes()}} to clip a bounding-box query to the place itself, so
that "Denver, Colorado" does not include Lakewood and Aurora.
}
\examples{
\dontrun{
den <- cl_boundary("Denver, Colorado")
plot(sf::st_geometry(den))
}
}
