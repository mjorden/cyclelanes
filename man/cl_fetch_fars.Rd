% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fars.R
\name{cl_fetch_fars}
\alias{cl_fetch_fars}
\title{Fatal crashes anywhere in the US from NHTSA's FARS}
\usage{
cl_fetch_fars(
  years,
  bbox,
  bike_only = TRUE,
  cache_dir = file.path(cl_cache_dir(), "fars")
)
}
\arguments{
\item{years}{Integer vector of years, 2001 or later.}

\item{bbox}{Area to keep (anything \code{\link[=cl_bbox]{cl_bbox()}} accepts, or an \code{sf}
polygon, which also clips).}

\item{bike_only}{Keep crashes in which a bicyclist was involved.}

\item{cache_dir}{Where the yearly zips are kept.}
}
\value{
An \code{sf} of \code{POINT}s in WGS84 with the \code{\link[=cl_fetch_crashes]{cl_fetch_crashes()}}
columns. \code{severity} is always \code{fatal}; \code{n_serious} is \code{NA} because
FARS records deaths only. \code{injured_mode} is the mode of the person who
died. \code{location_type} comes from the relation-to-junction code.
}
\description{
Every fatal crash on a US public road since 1975 is in the Fatality
Analysis Reporting System, with coordinates from 2001 on. This reads
the yearly national CSV release, keeps crashes inside \code{bbox}, and
returns them in the same shape as \code{\link[=cl_fetch_crashes]{cl_fetch_crashes()}}, so a city with
no crash source of its own still gets a fatalities-only layer.
}
\details{
Each year's release is a 20-40 MB zip, downloaded once into
\code{cache_dir}. FARS final data lag about two years. Coordinates recorded
as unknown (\code{77.7777}, \code{88.8888}, \code{99.9999} and their longitude
counterparts) are dropped, not plotted.
}
\examples{
\dontrun{
den <- cl_fetch_fars(2021:2022, bbox = cl_boundary("Denver, Colorado"))
table(den$injured_mode)
}
}
