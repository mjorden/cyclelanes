% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/official.R
\name{cl_fetch_official}
\alias{cl_fetch_official}
\title{Fetch a city's official bike-facility inventory}
\usage{
cl_fetch_official(
  city = "denver",
  existing_only = TRUE,
  bbox = NULL,
  page_size = 2000
)
}
\arguments{
\item{city}{A key from \code{\link[=cl_sources]{cl_sources()}}. Default \code{"denver"}.}

\item{existing_only}{Restrict to built facilities, dropping planned or
recommended ones, where the source can distinguish them.}

\item{bbox}{Optional area to limit the request: WGS84
\code{c(xmin, ymin, xmax, ymax)}, anything \code{\link[=cl_bbox]{cl_bbox()}} accepts, or an
\code{sf}/\code{sfc} \strong{polygon} such as the output of \code{\link[=cl_boundary]{cl_boundary()}}. The
bounding box is applied server-side for ArcGIS sources and after
reading for file sources; a polygon additionally clips the result to
its outline, so it matches a clipped \code{\link[=cl_bike_lanes]{cl_bike_lanes()}} layer.}

\item{page_size}{Records per request for ArcGIS sources. Capped at the
layer's advertised \code{maxRecordCount} (typically 1000 or 2000); paging
continues while the server reports \code{exceededTransferLimit}.}
}
\value{
An \code{sf} in WGS84 with columns \code{source_id}, \code{name},
\code{official_class} (the source's own label), \code{facility_type} (factor over
\code{\link[=cl_facility_levels]{cl_facility_levels()}}), any \code{extra_fields} from the source definition,
\code{length_m}, and geometry. \code{attr(x, "cl_source")} holds the city key and
\code{attr(x, "cl_attribution")} the provenance string.
}
\description{
Reads the registered source for \code{city} (see \code{\link[=cl_sources]{cl_sources()}}) and returns it
in the same shape and taxonomy as the OpenStreetMap output, so
\code{\link[=cl_summary]{cl_summary()}} and \code{\link[=cl_compare]{cl_compare()}} work on either.
}
\examples{
\dontrun{
den <- cl_fetch_official("denver")
cl_summary(den)
}
}
