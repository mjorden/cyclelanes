% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_source.R
\name{cl_check_source}
\alias{cl_check_source}
\title{Check a registered source's crosswalk against the live layer}
\usage{
cl_check_source(city, sample = 5000)
}
\arguments{
\item{city}{A key from \code{\link[=cl_sources]{cl_sources()}}.}

\item{sample}{For ArcGIS layers that cannot return distinct values,
how many rows to sample instead.}
}
\value{
A data frame of class \code{cl_source_check} with columns \code{value},
\code{status} (\code{mapped}, \code{unmapped}, \code{stale}), \code{target} (the taxonomy level
for mapped and stale rows) and \code{n} (feature count where the server
reports it). \code{attr(x, "ok")} is \code{TRUE} when nothing is unmapped, and
\code{attr(x, "city")} names the source.
}
\description{
Cities rename and add facility classes. A value missing from the
crosswalk silently becomes \code{none}. This fetches the distinct values of
the class field as they are today and reports which are mapped, which
are new (\code{unmapped}), and which crosswalk keys no longer occur
(\code{stale}). Run it before trusting a summary, and see the
\code{check-sources} workflow for a weekly automated run.
}
\examples{
\dontrun{
cl_check_source("denver")
cl_check_source("austin")
}
}
